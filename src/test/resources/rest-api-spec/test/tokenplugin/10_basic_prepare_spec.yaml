
---
"Basic prepare spec":
  - do:
      prepare_spec:
         body: {"features":[{"field":"some_field_name","tokens":"given","terms":["a","b","c"],"number":"tf", "type": "string"}]}

  - do:
      indices.refresh: {}

  - do:
      search:
         index: pmml
         type:  spec


  - match: {hits.total: 1}
  - match:  { hits.hits.0._index: pmml  }
  - match:  { hits.hits.0._type: spec  }

